---
export interface Props {
    title: string;
    color?: "primary" | "secondary";
}

const { title, color = "primary" } = Astro.props;
---

<button class=`button button--color-${color}`>
    {title}
</button>
